_schema-version: 3.3.0
ID: fy26_btp_cap_bootcamp_ticketing_system
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: fy26_btp_cap_bootcamp_ticketing_system-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: fy26_btp_cap_bootcamp_ticketing_system-auth
      - name: fy26_btp_cap_bootcamp_ticketing_system-db

  - name: fy26_btp_cap_bootcamp_ticketing_system-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: fy26_btp_cap_bootcamp_ticketing_system-db

resources:
  - name: fy26_btp_cap_bootcamp_ticketing_system-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: fy26_btp_cap_bootcamp_ticketing_system-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
  - name: fy26_btp_cap_bootcamp_ticketing_system-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
